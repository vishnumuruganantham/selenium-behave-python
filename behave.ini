[behave]
# Where Behave looks for feature files (default is ./features)
paths = features

# Stop the whole run on the first failing scenario instead of continuing
stop = false

# reports step/scenario durations.
show_timings = true

# Skip scenarios tagged @wip by default (work-in-progress)
tags = ~@wip

# Allure results for reporting. Don't also declare "pretty" here for the
# console output — behave already adds its own default console formatter
# automatically when none is configured. Explicitly listing "pretty" here
# (even matching the default) makes behave create it TWICE, so every line
# of output appears twice. Only the extra, non-default formatter belongs
# here.
format = allure
outfiles = reports/allure-results

# Option controls whether skipped steps/scenarios are printed
show_skipped = false

[behave.userdata]
# Custom key=value pairs YOU define, read in code via context.config.userdata
# Override on the command line, e.g.: behave -D browser=firefox -D env=prod
browser = firefox
env = prod

[behave.formatters]
# Register the Allure formatter under a short alias so 'format = allure' above resolves
allure = allure_behave.formatter:AllureFormatter