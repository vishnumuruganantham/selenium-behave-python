# Core
selenium==4.47.0
behave==1.3.3
allure-behave==2.16.1


# Test data handling
openpyxl==3.1.5        # read/write Excel test data (.xlsx)
PyYAML==6.0.2          # if config/test data is in YAML instead of .ini

# HTTP / API validation alongside UI tests
requests==2.32.3

# Utilities
python-dotenv==1.2.3   # load secrets from a .env file instead of hardcoding